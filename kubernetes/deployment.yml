kind: Deployment
apiVersion: apps/v1
metadata:
  name: my-app-deployment
  namespace: demo
  labels:
    app: flask
spec:
  replicas: 3
  selector:
    matchLabels:
      app: flask
  template:
    metadata:
      labels:
        app: flask
    spec:
      containers:
        - name: flask
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 80
